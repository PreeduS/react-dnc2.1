
//update later as user specific
export const actionTypes = {
    login : 'LOGIN',
    logout : 'LOGOUT'
};

const commonTypes = {
    status: {
        pending: '_PENDING',
        fulfilled: '_FULFILLED',
        rejected: '_REJECTED',
    }
}
export default commonTypes;