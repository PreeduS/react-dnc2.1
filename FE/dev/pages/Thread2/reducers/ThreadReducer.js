import initialState from './initialState';

const ThreadReducer = ( state = initialState.threadReducer, action) =>{
    switch(action.type) {}

    return state;
}

export default ThreadReducer;